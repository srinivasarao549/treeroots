define(["lib/compose"], function(compose){
 
    return compose(function(img){
            compose.call(this, {
                image: img,
                x:0,
                y:0,
                
                
            })
        })

})
